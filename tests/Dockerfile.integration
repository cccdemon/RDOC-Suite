FROM node:22-alpine

WORKDIR /tests

# ws for WebSocket tests
RUN npm install ws --no-save

COPY tests/integration/suite.test.mjs ./integration/

# Pass env vars at runtime:
#   docker run --rm --env-file /opt/RDOC-Suite/.env rdoc-suite-test
CMD ["node", "--test", "integration/suite.test.mjs"]
