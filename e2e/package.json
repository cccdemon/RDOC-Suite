{
  "name": "rdoc-suite-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright end-to-end suite for the Fleetplanner SPA, run against a live instance via the env-gated /e2e test-login seam.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
