// FR-P2 — API v1 contracts now live in the shared workspace package
// @rdoc-suite/fleetplanner-contracts (consumed by both the backend and the
// SPA). This module re-exports them so existing backend imports keep working.
export * from "@rdoc-suite/fleetplanner-contracts";
